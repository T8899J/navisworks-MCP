<Application x:Class="NavisworksMcp.Desktop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:NavisworksMcp.Desktop">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Disable WPF's dashed/dotted focus adorner globally while
                 preserving keyboard focus, caret, commands, and tab behavior. -->
            <Style x:Key="{x:Static SystemParameters.FocusVisualStyleKey}"
                   TargetType="{x:Type Control}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Control}">
                            <Grid IsHitTestVisible="False" />
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
            <local:InverseBoolConverter x:Key="InverseBoolConverter" />
            <local:ContextUsageDashArrayConverter x:Key="ContextUsageDashArrayConverter" />
            <local:ValuesEqualConverter x:Key="ValuesEqualConverter" />
            <local:ConversationWidthConverter x:Key="ConversationWidthConverter" />
            <local:GutterWidthConverter x:Key="GutterWidthConverter" />
            <local:GutterMarginConverter x:Key="GutterMarginConverter" />
        </ResourceDictionary>
    </Application.Resources>
</Application>
