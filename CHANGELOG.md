# 版本信息

> 记录桌面端（`navisworks-desktop-electron`）的功能更新与 bug 修复。
> **每次更新追加到顶部**，最新版本在最上。更早的既存功能见 `README.md` 与 `docs/`。

格式：`版本号 — 日期`，分「新增 / 优化 / 修复 / 移除」四类。

---

## 未发布 — 2026-09-01

### 新增

- Agent Runtime 引入 ContextFrame、有限 token 预算、Verified Facts、Reference Sets、Tool Result Recall、
  Semantic Memory、持久执行账本与 Cordis Scope 生命周期。
- Navisworks 状态增加 `bridgeSessionId` 与 `documentInstanceId`，文档切换后自动使旧事实、引用和审批失效。
- 插件端点改为 `endpoints\{ProcessId}.json`；Electron 增加多实例发现、显式选择与不可变 Run Binding，
  审批和执行账本按实例及文档隔离，运行中不自动切换或故障转移。
- 超过 256 KiB 的工具结果外置到桌面数据目录，并保留会话内预览与内部召回能力。
- Composer 推理强度条改为 WebGL2 实时流光：域扭曲 fbm 光带随档位提速、随拖拽增强，
  五档累计保留青、蓝、靛到深紫的颜色；进入 Max 时流体、流速和气泡增强 3 秒。无 WebGL2 时
  回落到同档位的静态 CSS 渐变，未填充区域仅保留低对比方形 LED 纹理。

### 优化

- 本地 Ollama 与用户显式选择的 OpenAI 兼容 API 共用同一套上下文预算和工具循环；API 不再被假设为 1M 窗口。
- Conversation 与 Document 使用正交 Scope；Run 只在本次执行中引用两者。

### 修复

- Compact 和预算裁剪不再拆开 assistant tool call 与对应 tool results，也不会删除当前用户指令。
- 修改型工具在审批后和取得执行锁后再次检查文档身份；崩溃或不确定结果不会自动重复执行。
- 文档切换会立即取消绑定旧文档的待审批操作。

### 移除

- 云救援功能及其事件、设置和文档入口。

## v0.2.0-dev.0 — 2026-08-27

### 新增

- **对话区左侧大纲导航条**：每轮对话（一次发送 + 一次回复）合并成一根横条；悬停触发双向对称波峰——光标所在的横条为最高点，向上下两侧依次衰减，带波浪式延迟。
- **导航条预览窗**：悬停某轮横条时弹出卡片，上行显示该轮用户消息（粗体深色），下行显示 agent 回复（浅色），均单行省略，字号与正文一致。
- **拖动导航条 scrub 对话区**：按住导航条上下拖动，对话区按比例跟手滚动；使用 Pointer Events + `setPointerCapture`，光标移出导航条进入对话区时波峰仍跟随投影定位。
- **导航条碰撞隐藏**：对话区左缘碰到导航条即隐藏（`ResizeObserver` + `resize` 实时测量，不依赖固定窗口宽度断点）；导航条绝对定位不占文档流，显示/隐藏不抖动。
- **思考过程 / 工具调用与回复之间的分隔线**：淡色细横线（`--border` 40% 透明度），贯穿对话列宽度但不超出用户气泡右缘。
- **消息与输入框之间的安全距离**：消息区底部预留 `输入框高度 + 36px`，最新消息不再紧贴输入框。
- **标题行搜索弹窗**：点侧栏「Curi」旁的放大镜弹出全窗口搜索弹窗（720px 居中、毛玻璃遮罩）；上方搜索框，下方按置顶/时间排列全部会话，点选直达；`Esc` 或点遮罩关闭。
- **会话列表日期分组**：会话区按「置顶 / 今天 / 昨天 / 最近」小节标签分组（自然日划分，空组不显示）。
- **新建会话随机问候语**：预设 10 条聊天/工作/执行任务风格的主标题，每次新建随机切换。
- **会话标题自动总结**：发送首条消息后，模型在后台把标题概括成不超过 12 字的短语（截断文案先作占位）；超时或提供商停用时保留截断。
- **Navisworks 状态自动检测**：main 进程每 5 秒轮询插件状态，上下线 5 秒内自动更新，无需手动刷新。
- **发送按钮空输入置灰**：无内容时灰色不可用、hover 不发亮，不再出现「禁止」光标。

### 优化

- **工具调用展示形态**：从整行横条卡片改为与思考过程一致的流式平铺形态（小标题行 + 左竖线缩进正文），右缘严格停在对话列边界，不超出用户气泡。
- **窗口启动尺寸**调整为 1366×828。
- **侧栏头部改版**：标题仅「Curi」（24px 加粗，上下留白对称）；「新对话」按钮改用 SquarePen 图标，高度与会话条一致。
- **模型/思考程度菜单**：新建会话界面下改为向下展开，不再遮挡问候语；触发条与展开面板字体统一 12px、名称 600 加粗。
- **设置外观字体滑块**：刻度节点改空心圆圈骑在连续轨道上，刻度文字与节点精确对齐，轨道两端不超出节点。
- **hero 主标题**字距放宽（`letter-spacing: 0.06em`），不再拥挤。
- **`navisworks_find_items` 可恢复分块扫描**（插件端）：单次扫描改到 UI 线程分片运行（500 ms 切片 + 10 s 预算），结果 `truncated` 为 true 时用完全相同的参数再次调用可从断点继续；扫描上限从 10 万提到 200 万，超大 federated 模型（50 万+ 构件）不再搜不到尾部。`navisworks_list_viewpoints` 同步增加 `limit`/`offset` 分页，结果带 `total` 总数。

### 修复

- **回底按钮被输入框遮挡**：dock 的不透明渐变遮罩盖住了按钮——改为悬浮在输入框上沿之上、层级提升到 dock 之上，并跟随输入框高度自动上浮。
- **进入历史会话时的滚动动画**：原 `scroll-behavior: smooth` 导致从顶滚到底的动画——切换/首次进入会话改为 `'instant'` 即时显示最新消息，同会话内新消息到达仍保持平滑跟随。
- **删除当前会话后的去向**：不再自动跳入其他会话，改为落在新建会话界面。

### 移除

- 消息上方的头像图标与角色名（「图标 + 你」「图标 + 助手」），仅保留 `aria-label` 供读屏。
- 输入区上方的常驻搜索框（由标题行搜索弹窗取代）。
- hero 副标题说明行与写死的「开始处理 Navisworks 任务」标题。
- 侧栏「新建会话」旧文案与 `MessageSquarePlus` 图标。
